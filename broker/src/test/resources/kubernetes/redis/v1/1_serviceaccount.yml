apiVersion: v1
kind: ServiceAccount
metadata:
  name: operator
  namespace: {{SERVICE_ID}}
  labels:
    service_type: redis
    service_id: {{SERVICE_ID}}
    org: {{ORG_ID}}
    space: {{SPACE_ID}}
