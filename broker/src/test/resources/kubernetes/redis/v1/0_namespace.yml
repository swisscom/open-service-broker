apiVersion: v1
kind: Namespace
metadata:
  name: {{SERVICE_ID}}
  labels:
    service_id: {{SERVICE_ID}}
    service_type: redis-sentinel
    space: {{SPACE_ID}}
    org: {{ORG_ID}}
  annotations:
    appcloud.swisscom.com/disk-quota: "1g"
